# Docs

User-facing and contributor documentation (the privacy policy in plain English, an
onboarding page, contributor notes). The internal spec series (`00-*`..`11-*`) and the
ADRs are **not** here and are never committed — they stay local per workspace policy.
