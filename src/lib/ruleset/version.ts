// The companion ruleset (kvadrum/read-twice-rules) is SemVer'd independently of the
// extension (architecture §11). The extension declares the minimum version it supports;
// the bundled ruleset is whatever was current at build time.
export const RULESET_VERSION = '0.0.0';
export const MIN_SUPPORTED_RULESET_VERSION = '0.0.0';
